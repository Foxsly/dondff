/**
 * @packageDocumentation
 * @module api.functional.sleeper.stats
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { SleeperStatRequest } from "@/sleeper/entities/sleeper.entity";
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import type { Primitive, Resolved } from "typia";

import type { SleeperStatResponse } from "../../../../../../sleeper/entities/sleeper.entity";

/**
 * @controller SleeperController.getStats
 * @path GET /sleeper/stats/:year/:week
 * @accessor api.functional.sleeper.stats.getStats
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getStats(
  connection: IConnection,
  year: number,
  week: number,
  query: getStats.Query,
): Promise<getStats.Output> {
  return PlainFetcher.fetch(connection, {
    ...getStats.METADATA,
    template: getStats.METADATA.path,
    path: getStats.path(year, week, query),
  });
}
export namespace getStats {
  export type Query = Resolved<SleeperStatRequest>;
  export type Output = Primitive<SleeperStatResponse>;

  export const METADATA = {
    method: "GET",
    path: "/sleeper/stats/:year/:week",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (year: number, week: number, query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = `/sleeper/stats/${encodeURIComponent(year?.toString() ?? "null")}/${encodeURIComponent(week?.toString() ?? "null")}`;
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
